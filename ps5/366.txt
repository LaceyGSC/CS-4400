
push	%ebp
mov		%esp, %ebp
mov		0x8(%ebp), %eax
mov		0xc(%ebp), %eax
lea		(%eax,%eax,4), %eax
add		0x4(%ecx,%eax,4), %eax
mov		0xb8(%ecx), %edx
add		(%ecx), %edx
mov		%edx, 0x8(%ecx,%eax,4)
pop		%ebp
ret



typedef struct {
	int left;
	a_struct a[CNT];
	int right;
} b_struct;

void test(int i, b_struct *bp)
{
	int n = bp->left + bp->right;
	a_struct *ap = &bp->a[i];
	ap->x[ap->idx] = n;
}